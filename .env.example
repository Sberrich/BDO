# Delivery: formsubmit (any inbox, no domain) | resend (needs verified domain for other recipients)
EMAIL_PROVIDER=formsubmit

# Inbox that receives form submissions (e.g. certificat@bdo-info.ma)
NOTIFY_EMAIL=certificat@bdo-info.ma

# Optional — only if EMAIL_PROVIDER=resend
# RESEND_API_KEY=re_xxxxxxxx
# FROM_EMAIL=BDO Certificat <onboarding@resend.dev>

# Public contact on the site is certificat@bdo-info.ma / +212 661 448 496
# (see web/lib/content.ts and web/lib/payload/site.ts).
